package _3CondicaoTernaria;

public class Main {
    public static void main(String[] args) {
        // Ternario expressão opcional if else quando deseja decidir um VALOR com base em uma condição
        // (condicao) ? valor se verdadeiro : valor se falso
        int x = 10;
        int multiplica = (x >= 10) ? x * 1 : x * 2;
    }
}
